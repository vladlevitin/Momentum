# Put all modules that aren't standard in the Python library here.
# This is for the GitLab pipeline to install the needed resources before running our tests.
#

Django==2.1.7
django-crispy-forms==1.7.2
Pillow==5.3.0
googlemaps==3.0.2
django-geoposition==0.3.0
coverage==4.5.2
